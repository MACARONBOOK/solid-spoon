<h1>New book</h1>
  <%= form_with model: @book do |f| %>
    <div class="field">
    <h4>Title</h4>
      <%= f.text_field :title %>
    </div>
    <div class="field">
    <h4>Opinion</h4>
      <%= f.text_area :body %>
      <%= f.submit 'Create Book'%>
    </div>
  <% end %>